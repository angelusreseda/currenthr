<!-- leuke test, moest alleen nog een doctype: json worden ?text=HR%200&icon=i15434&index=0 erbij in de adresbalk -->
<pre id="json"></pre>

<script>
	
	function QueryStringToJSON() {      
		var pairs = location.search.slice(1).split('&');
		
		var result = {};
		pairs.forEach(function(pair) {
			pair = pair.split('=');
			result[pair[0]] = decodeURIComponent(pair[1] || '');
		});

		return JSON.parse(JSON.stringify(result));
	}
	
	var qs = QueryStringToJSON();

	document.getElementById("json").innerHTML = '{"frames": ['+JSON.stringify(qs)+']}';

</script> 

